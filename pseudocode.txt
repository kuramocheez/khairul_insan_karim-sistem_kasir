PROGRAM sistem_kasir_sederhana

CREATE OBJECT "item" with property:
    itemId integer,
    itemName string,
    price integer

SET listItem with []item
SET totalHarga = 0

WHILE range of item
    totalHarga = item[price] * ammount
END WHILE


WHILE "trancsaction" == true
    READ AND WRITE "price" with number
    STORE "totalPayment" with totalPayment plus price
    READ AND WRITE "trancsaction"

DISPLAY totalPayment